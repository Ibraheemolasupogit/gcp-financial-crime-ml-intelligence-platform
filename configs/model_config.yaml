# Placeholder model configuration. No models are implemented in Milestone 1.
models:
  fraud_detection:
    enabled: false
  aml_risk_scoring:
    enabled: false
  anomaly_detection:
    enabled: false

